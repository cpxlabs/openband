- [ ] Add `computeModulation()` call inside `renderMixdown()` per plugin param.
- [ ] Implement LFO-as-`ConstantSourceNode` → `AudioParam` connection for live playback.
- [ ] Implement env/macro as setValueAtTime curves during offline render.
- [ ] Wire `OneKnob.tsx` mod affordance to write `matrix.routes[]`.
- [ ] Show active routes in `PluginEditor.tsx`.
- [ ] Add Vitest test: LFO route changes param value over time within expected range.
- [ ] Manual: LFO→cutoff audible sweep; macro→2 params move together.
